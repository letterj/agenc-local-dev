services:
  agenc-creator:
    build:
      context: .
      dockerfile: Dockerfile
    platform: linux/amd64
    container_name: agenc-creator
    ports:
      - "3100:3101"
    environment:
      - AGENC_PROGRAM_ID=GN69CoBM1XUt8MJtA6Kwd7WRwLzTNtVqLwf5o3fwWDV3
    volumes:
      - agenc-creator-data:/root/.agenc
      - ./docker/creator/config.json:/root/.agenc/config.json:ro
      - ${HOME}/.config/solana/id.json:/root/.config/solana/id.json:ro
    stdin_open: true
    tty: true

  agenc-worker:
    build:
      context: .
      dockerfile: Dockerfile
    platform: linux/amd64
    container_name: agenc-worker
    ports:
      - "3101:3101"
    environment:
      - AGENC_PROGRAM_ID=GN69CoBM1XUt8MJtA6Kwd7WRwLzTNtVqLwf5o3fwWDV3
    volumes:
      - agenc-worker-data:/root/.agenc
      - ./docker/worker/config.json:/root/.agenc/config.json:ro
      - ${HOME}/.config/solana/worker.json:/root/.config/solana/id.json:ro
    stdin_open: true
    tty: true

volumes:
  agenc-creator-data:
    driver: local
  agenc-worker-data:
    driver: local
